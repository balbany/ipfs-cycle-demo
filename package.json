{
  "name": "ipfs-cycle-demo",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cycle-scripts": "1.1.0"
  },
  "dependencies": {
    "@cycle/dom": "^18.2.0",
    "@cycle/run": "^3.2.0",
    "ipfs-pubsub-room": "^0.1.5",
    "xstream": "^10.9.0"
  },
  "scripts": {
    "start": "cycle-scripts start",
    "test": "cycle-scripts test",
    "build": "cycle-scripts build",
    "eject": "cycle-scripts eject",
    "deploy": "cycle-scripts build && UploadToAz.bat"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
